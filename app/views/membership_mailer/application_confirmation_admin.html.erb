<%= raw(@email.body) %>

<h2>Member Details</h2>

<p>
  <% @member.attributes.each do |k, v|
    unless v.blank? || k =~ /password|username|updated/%>
      <b><%= "#{k.humanize}:"%></b><%=v%><br/>
  <%end
  end %>
</p>

<p>
Edit Profile: <%= link_to edit_members_url(:id => @member.id), edit_members_url(:id => @member.id) %>
</p>

<%= raw(@email.body) %>

<p>
  Canceled Member's Profile: <%= edit_members_url(:id => @member.id) %>
</p>

<h2>Member Details</h2>
<p>
  <% @member.attributes.each do |k, v|
    unless v.blank? || k =~ /password|username|updated/%>
      <b><%= "#{k.humanize}:"%></b><%=v%><br/>
  <%end
  end %>
</p>

